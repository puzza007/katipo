sudo: true

language: erlang

otp_release:
  - 17.5
  - 18.0
  - 18.1

script: make update test dialyzer

before_install:
  - ./travis_install_curl.sh

addons:
  apt:
    packages:
      - libevent-dev
  # artifacts:
  #   paths:
  #     - $TRAVIS_BUILD_DIR/_build/test/logs/
