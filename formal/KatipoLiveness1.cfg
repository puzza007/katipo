\* Liveness with sends-to-dead-worker disabled: any violation must come
\* from a cast that reached a live worker and was then lost.
SPECIFICATION Spec
CONSTANTS
  Refs = {r1}
  MaxPortDeaths = 1
  AllowDeadSend = FALSE
PROPERTIES
  EventualOutcome
