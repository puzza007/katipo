\* Cancel-pollution check: expected to FAIL (gap: a cancel processed while
\* the port is closed crashes the worker via port_command badarg before
\* maps:remove, so terminate delivers worker_died to the cancelling caller).
SPECIFICATION Spec
CONSTANTS
  Refs = {r1}
  MaxPortDeaths = 1
  AllowDeadSend = FALSE
INVARIANTS
  NoCancelPollution
